# Environment Configuration for Local Development

# Node Environment
NODE_ENV=development

# Database Configuration (Optional for local development)
# If you want to use PostgreSQL locally, set these values:
# DATABASE_URL=postgresql://username:password@localhost:5432/techorbeez
# PGHOST=localhost
# PGPORT=5432
# PGUSER=your_username
# PGPASSWORD=your_password
# PGDATABASE=techorbeez

# Note: If DATABASE_URL is not set, the application will automatically
# use in-memory storage for local development